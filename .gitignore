# Nunca subir credenciales locales (tokens OAuth, etc.) al repo público.
.secrets/

# Backlog #221 (9 sep): copias de seguridad locales del estado real de
# Firestore, independientes del backup que el propio sitio ya guarda
# DENTRO de Firestore (útiles justo si Firestore mismo fallara). Pueden
# contener datos reales del canal — nunca al repo público.
.backups-local/

# Claude Code local config
.claude/

# Backlog: audio generado por scripts/generate-retro365-audio.js — son
# archivos de salida (audio), no código fuente, y pesan demasiado para
# el repo. Se entregan a Iván con SendUserFile, no se commitean.
retro365-audio/
retro365-audio*.zip
node_modules/
